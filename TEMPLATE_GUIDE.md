# 오답 아카이브 사용 안내 📘

**「오답 아카이브」는 오답노트 정리 봇이 기록하는 노션 표에 공부 관리 화면을 함께 붙인 유료 노션 템플릿입니다.**

텔레그램으로 보낸 오답이 쌓이는 표는 무료 템플릿과 같고,
그 옆에 디데이·학습 캘린더·월간 목표·회독수 트래커·취약 단원 분석 같은 화면이 한 페이지에 모여있습니다.
기록을 넣어 주는 봇은 [오답노트 정리 봇](https://github.com/git-Jynx/ErrorLog-Bot)과 **완전히 같은 프로그램**이며,
이 템플릿 때문에 바뀐 것은 없습니다.
무료 템플릿만으로도 봇의 기능은 전부 쓸 수 있으니, 이 템플릿은 공부 관리 화면까지 한곳에서 보고 싶은 분을 위한 선택지입니다.

이 문서는 템플릿을 처음 복제해 봇과 연결할 때 **위에서부터 순서대로** 따라가는 안내입니다.
쓰다가 막혔을 때는 템플릿 안의 `사용법 QnA`도 함께 보세요.

---

## 1. 이 템플릿으로 할 수 있는 것

메인 페이지 한 장에 아래 화면이 모여 있습니다.

| 화면 | 보여 주는 것 |
| --- | --- |
| 디데이 · 오늘의 한마디 | 시험일까지 남은 날짜와 한마디 |
| 학습 캘린더 · 오늘 할 일 | 공부 일정을 달력으로, 오늘 할 일을 목록으로 |
| 월간 목표 | 이번 달 목표와 진행률 막대 |
| 회독수 트래커 | 과목·교재별 1~5회독 체크 |
| 취약 단원 분석 | 단원별로 틀린 건수 (그래프 + 보드) |
| 오늘의 복습 | 기록한 지 7일째 되는 오답 |
| 최근 추출본 | `/export`로 노션에 저장한 정리본 |
| 사용법 QnA | 쓰다가 막히기 쉬운 지점의 질문과 답 |

메인 페이지에 보이는 화면은 대부분 **연동 뷰**(다른 곳에 있는 표를 이 페이지에서 창처럼 보여 주는 것)입니다.
표의 원본은 메인 페이지 안의 `🗄 데이터 보관소`라는 하위 페이지에 모여 있습니다.

그중 **봇이 직접 쓰는 표는 두 개**뿐입니다.

| 표 이름 | 있는 곳 | 봇이 하는 일 | `.env` 항목 |
| --- | --- | --- | --- |
| `오답노트 저장소` | 메인 페이지 | `/new`로 보낸 기록을 저장 | `NOTION_DB_ID` |
| `📤 CPA 추출본` | `🗄 데이터 보관소` 안 | `/export` 정리본을 저장 | `NOTION_EXPORT_DB_ID` |

나머지 표(`⚙️ 대시보드 설정`, `📅 학습 일정`, `🎯 월간 목표`, `📖 회독수 트래커`)는 봇과 상관없이 내가 노션에서 직접 채우는 표입니다.

---

## 📝 2. 복제하기

**복제**는 판매용 원본 페이지를 내 노션에 통째로 사본으로 만드는 것입니다.
원본은 그대로 두고, 내 사본에서만 작업합니다.

1. 크티에서 구매한 뒤 받은 템플릿 링크를 웹브라우저에서 엽니다.
   ({{채워넣기: 구매 후 템플릿 링크를 받는 위치}})
2. 화면 **우측 상단의 `복제`**(영문 노션은 `Duplicate`)를 누릅니다.
   노션 로그인을 요구하면 로그인합니다.
3. 어느 **워크스페이스**(내 노션 작업공간)에 넣을지 물으면 내 것을 고릅니다.

성공 신호: 내 노션 왼쪽 사이드바에 템플릿 페이지가 새로 생기고, 열면 디데이와
캘린더가 있는 메인 페이지가 보입니다.

⚠️ **앞으로 복사하는 표 주소는 전부 "내 사본"에서 복사해야 합니다.** 복제하면 표의
주소(ID)가 새로 만들어지므로, 판매 페이지 원본의 주소는 봇에 쓸 수 없습니다.

---

## 🔑 3. 봇 연결하기

봇을 아직 설치하지 않았다면 §3-1, 이미 무료 템플릿으로 봇을 쓰고 있다면 §3-2를
따르세요. §3-3과 §3-4는 두 경우 모두 해야 합니다.

### 3-1. 봇이 아직 없다면

봇 설치는 [오답노트 정리 봇 저장소](https://github.com/git-Jynx/ErrorLog-Bot)의
`README.md`를 처음부터 순서대로 따라가세요. 초보자 기준으로 설치 과정이 전부 적혀
있습니다. 이 템플릿을 쓸 때 README와 **다르게 하는 곳은 네 군데**입니다.

1. **README §2 (노션 표 준비하기):** 표를 새로 만들지 않습니다. 이 템플릿을 이미
   복제했기 때문입니다. 통합 연결은 README §2-5 대신 이 문서의 §3-3을 따르세요.
2. **README §4의 ③번 (`.env` 채우기):** `NOTION_EXPORT_DB_ID`를 비워 두지 말고, 이
   문서의 §3-4를 보고 채웁니다.
3. **README §4의 ⑤번 (`/setup`으로 표 고르기):** 버튼 목록에서
   **`오답노트 저장소`**를 고릅니다. 다른 표를 고르면 봇이 "학습 기록 표의 규격과
   맞지 않습니다"라고 알려 주고 저장하지 않으니, 그때는 `/setup`을 다시 보내
   고르면 됩니다.
4. **이 문서의 §4 (처음 설정할 것):** 설치가 끝난 뒤 시험일 입력과 샘플 데이터
   정리를 합니다.

### 3-2. 이미 봇을 쓰고 있다면

봇 프로그램은 그대로 두고, **봇이 기록할 표 두 개만 새 템플릿의 표로 바꿉니다.**
설치를 다시 할 필요는 없습니다.

1. 이 문서의 **§3-3 통합 연결**을 먼저 합니다. 새로 복제한 페이지에는 아직 봇의
   출입이 허락되어 있지 않습니다.
2. 봇을 켠 상태에서 텔레그램으로 **`/setup`**을 보내고, 버튼 목록에서 새 템플릿의
   **`오답노트 저장소`**를 고릅니다. `.env`의 `NOTION_DB_ID`가 새 표 주소로 자동으로
   바뀌고, 고른 즉시 새 표에 저장하기 시작합니다.
3. 이 문서의 **§3-4**를 보고 `NOTION_EXPORT_DB_ID`의 값을 새 템플릿의
   `CPA 추출본` 표 주소로 **바꿔 적습니다.** 이미 봇을 써 왔다면 여기에는 예전
   추출본 표의 주소가 적혀 있으므로, 그대로 두면 정리본이 계속 예전 표로 갑니다.
4. 봇을 껐다 켭니다. (터미널에서 `Ctrl + C`를 누르고 `python bot.py`)

알아 두실 점이 두 가지 있습니다.

- **예전 표에 있던 기록은 새 표로 자동으로 옮겨지지 않습니다.** 예전 표는 지워지지
  않고 그대로 남아 있으니, 필요할 때 열어 보시면 됩니다.
- 저장할 때 봇이 알려 주는 "이 개념을 몇 번째 틀렸는지" 횟수는 **새 표에 쌓인
  기록만** 셉니다.

`/setup` 목록에 이름이 같은 표가 두 개 보여 어느 것이 새 표인지 헷갈린다면,
README §3-4의 "직접 입력하고 싶을 때" 방법으로 새 `오답노트 저장소`의 주소를
`NOTION_DB_ID=` 뒤에 직접 적어도 됩니다. 주소를 복사하는 방법은 아래 §3-4와
같습니다.

### 3-3. 노션 통합 연결하기

**통합**(Integration)은 봇 같은 외부 프로그램이 내 노션에 들어올 수 있게 만든
출입증입니다. 통합을 만드는 방법은 README §3-2에 있습니다. 통합을 만들었다고 봇이
바로 표를 볼 수 있는 것은 아니고, **페이지마다 출입을 허락해 주어야 합니다.**

**복제한 템플릿의 맨 위 페이지(메인 페이지)에 연결하세요.** 맨 위 페이지에 연결하면
그 아래에 들어 있는 페이지와 표(`🗄 데이터 보관소` 안의 표 포함)까지 한 번에
허락됩니다.

1. 노션에서 복제한 템플릿의 메인 페이지를 엽니다.
2. **우측 상단의 `···`** 버튼을 누릅니다.
3. **`연결`**(영문 노션은 `Connections`)을 누릅니다.
4. README §3-2에서 만든 통합의 이름을 찾아 누릅니다. (예: `오답노트봇`)
5. 확인 창이 뜨면 `확인`을 누릅니다.

성공 신호: 봇을 켠 뒤 텔레그램에서 `/setup`을 보냈을 때 버튼 목록에
`오답노트 저장소`가 보입니다. 보이지 않는다면 이 단계를 다른 페이지에 했을
가능성이 큽니다.

### 3-4. `NOTION_EXPORT_DB_ID` 설정하기 — 유료 템플릿 전용 단계

무료 템플릿에서는 비워 두는 값이지만, **이 템플릿에서는 직접 채워야 합니다.**

**왜 필요한가:** 봇은 `/export` 정리본을 노션에 저장할 때 `.env`의
`NOTION_EXPORT_DB_ID`를 먼저 봅니다. 값이 있으면 그 표에 저장하고, 비어 있으면 추출본
표를 **새로 만들어** 그 주소를 `.env`에 적어 둡니다. 이 템플릿에는 추출본 표가 이미
들어 있으므로, 그 주소를 알려 주지 않으면 봇이 표를 하나 더 만들게 됩니다. 새 표는
메인 페이지 아래쪽에 따로 생기고, 정리본이 대시보드의 `최근 추출본` 화면이 아니라
그 새 표에 쌓입니다.

**1) 표 주소 복사하기**

1. 메인 페이지에서 **`🗄 데이터 보관소`** 페이지를 엽니다.
2. 그 안의 **`📤 CPA 추출본`** 표를 엽니다. 표가 페이지 일부로 끼워져 보이면
   **표 이름을 눌러** 표만 화면 가득 엽니다.
   (메인 페이지의 `최근 추출본` 화면은 연동 뷰이므로, 주소는 원본 표에서 복사하는
   것이 확실합니다.)
3. 화면 **우측 상단의 `···`**을 누르고 **`링크 복사`**를 누릅니다.
4. 복사한 주소를 메모장 같은 곳에 붙여넣어 봅니다. 이런 모양입니다.

```
https://www.notion.so/내작업공간/8f2c1d4e5a6b7c8d9e0f1a2b3c4d5e6f?v=...
```

**`notion.so/` 다음부터 `?v=` 이전까지** 중에서, 물음표(`?`) 바로 앞의 마지막
토막 **32자리**가 표의 주소입니다. 위 예시라면 `8f2c1d4e5a6b7c8d9e0f1a2b3c4d5e6f`입니다.
주소에 표 제목이 붙어 있으면(예: `CPA-8f2c...5e6f`) **마지막 하이픈(`-`) 뒤부터**가
32자리입니다.

**2) `.env`에 적기**

1. 프로젝트 폴더의 `.env` 파일을 엽니다. (`.env`는 봇의 비밀 설정값을 적어 두는
   파일입니다. 여는 방법은 README §4의 ③번에 있습니다.)
2. `NOTION_EXPORT_DB_ID=` 줄을 찾아 `=` 바로 뒤에 32자리를 붙여넣습니다.

```
NOTION_EXPORT_DB_ID=8f2c1d4e5a6b7c8d9e0f1a2b3c4d5e6f
```

- ⚠️ `=` 뒤에 공백을 넣지 마세요. 따옴표로 감싸지도 마세요.
- 저장 단축키는 맥이 `Cmd + S`, 윈도우가 `Ctrl + S`입니다.

**3) 봇 껐다 켜기**

이 값은 **봇이 켜질 때 한 번만 읽습니다.** 봇이 켜져 있었다면 터미널에서
`Ctrl + C`(실행 중인 프로그램을 끄는 단축키)를 누르고 다시 켭니다.

```bash
python bot.py
```

**성공 신호:** 텔레그램에서 `/export`를 보내 `[📄 노션에 저장]`을 눌렀을 때,

- 대시보드의 `최근 추출본` 화면에 정리본이 나타나고,
- 봇을 켠 터미널 창에 `🆕 추출본 표 'CPA 추출본'을(를) 만들었습니다.`라는 줄이
  **나오지 않으면** 성공입니다.

이 줄이 나왔다면 봇이 표를 새로 만든 것입니다. §6의 "추출본 표가 하나 더
생겼어요"를 보세요.

> `/export`를 해 보기 전에 미리 확인하고 싶다면, 프로젝트 폴더에서 가상환경에
> 들어간 상태로 아래 검사 도구를 실행하세요. 노션에 아무것도 쓰지 않고 읽기만
> 합니다.
>
> ```bash
> python check_notion.py
> ```
>
> 결과에 ``[완료] 추출본 표 `CPA 추출본` 속성 5개 확인됨``이 보이면 주소가 맞게
> 들어간 것입니다.

---

## 4. 처음 설정할 것

### 4-1. 시험일 입력하기

디데이와 오늘의 한마디는 **시험일**을 기준으로 계산됩니다.

1. 메인 페이지의 `⚙️` 설정 카드를 찾습니다. (원본 표는 `🗄 데이터 보관소` 안의
   `⚙️ 대시보드 설정`입니다.)
2. `시험일` 칸을 누르고 달력에서 날짜를 고릅니다.
3. `수험 시작일`은 선택 사항입니다. 넣고 싶으면 같은 방법으로 고릅니다.

성공 신호: 디데이 자리에 남은 날짜가 나타납니다. 시험일이 비어 있으면
`시험일을 입력하세요`라고 표시됩니다.

이 설정 표는 **행이 1개인 것이 정상입니다.** 행을 새로 추가하지 말고, 있는 한
줄의 값만 고치세요.

### 4-2. 샘플 데이터 지우기

템플릿에는 화면이 어떻게 보이는지 보여 주기 위한 예시 행이 들어 있습니다.

**첫 기록을 저장하기 전에 지우는 것을 권합니다.** 특히 `오답노트 저장소`에 예시
기록이 남아 있으면, 취약 단원 분석의 건수에 예시가 함께 세어집니다.

1. 지울 행에 마우스를 올립니다.
2. 행 왼쪽에 나타나는 `⋮⋮` 손잡이를 누릅니다.
3. `삭제`를 누릅니다.

⚠️ **행만 지우세요. 표 자체나 속성(칸)은 지우지 마세요.** 표를 지우면 그 표를
보여 주던 대시보드 화면이 함께 비어 버립니다.

### 4-3. 내 교재 목차 넣기

봇의 과목·단원 버튼은 봇 쪽의 `chapters.yaml` 파일에서 만들어집니다. 내 교재
목차로 바꾸는 방법은 README §8-1에 있습니다. 노션 쪽에서는 따로 할 일이 없습니다.

---

## 💡 5. 화면별 사용법

### 5-1. 디데이 · 오늘의 한마디

§4-1에서 시험일을 넣으면 매일 자동으로 바뀝니다. 시험일이 바뀌면 설정 카드의
`시험일`만 고치면 됩니다.

### 5-2. 학습 캘린더 · 오늘 할 일

두 화면은 `📅 학습 일정`이라는 **같은 표를 두 가지 모양으로** 보여 줍니다. 그래서
한쪽에 추가한 일정은 다른 쪽에도 함께 나타납니다. 달력의 날짜 칸이나 목록 아래의
`+ 새로 만들기`를 눌러 일정을 추가합니다.

### 5-3. 월간 목표

이번 달 목표와 진행률 막대를 보여 줍니다.

⚠️ **매달 초에 이 화면의 필터를 한 번 바꿔 주어야 합니다.** `🎯 월간 목표` 표의
`월` 속성이 선택형이라, 노션이 "이번 달"을 스스로 알아내지 못하기 때문입니다.

1. 월간 목표 화면 **우측 상단의 `···`**(뷰 설정)을 누릅니다.
2. `필터`를 누릅니다.
3. `월` 조건의 값을 이번 달로 바꿉니다.

**필터**는 표에서 조건에 맞는 행만 골라 보여 주는 설정입니다. 필터를 바꿔도
기록은 지워지지 않습니다.

### 5-4. 회독수 트래커

과목·교재별로 1회독부터 5회독까지 체크하는 표입니다. 교재를 한 번 다 볼 때마다
해당 칸을 체크하세요. 새 교재나 새 과목은 행을 추가해 넣습니다. **이 표는 봇과
상관이 없어, `chapters.yaml`에 과목을 추가해도 자동으로 행이 생기지 않습니다.**

### 5-5. 취약 단원 분석

`오답노트 저장소`에 쌓인 기록을 **단원별 건수로** 보여 줍니다. 위쪽이 그래프,
바로 아래가 같은 내용을 담은 보드입니다. 봇이 기록을 저장하면 자동으로 반영되므로
따로 할 일은 없습니다.

- 노션에는 건수가 많은 순으로 자동으로 줄을 세우는 기능이 없습니다. **단원의
  순서는 건수 순이 아니므로, 각 단원 옆의 숫자를 보세요.** 숫자는 항상 정확합니다.
- 그래프 자리에 잠금 표시가 뜨면 §8-2를 보세요. 그래프가 없어도 바로 아래 보드에서
  같은 정보를 볼 수 있습니다.

### 5-6. 오늘의 복습

기록한 지 **정확히 7일째 되는 날**, 그날 복습할 오답을 보여 줍니다.

이 화면을 위해 `오답노트 저장소`에는 무료 템플릿에 없는 **속성 2개가 더 있습니다.**
둘 다 이 템플릿의 화면용이며, **봇은 이 두 칸을 읽지도 쓰지도 않습니다.**

| 속성 이름 | 종류 | 하는 일 |
| --- | --- | --- |
| `복습예정일` | 수식(formula) | `기록일`에 7일을 더한 날짜를 자동으로 계산합니다. 수식은 `dateAdd(prop("기록일"), 7, "days")`입니다 |
| `복습완료` | 체크박스(checkbox) | 복습을 마쳤는지 표시하는 칸입니다. 기본 화면에서는 쓰지 않습니다 |

**수식**은 다른 칸의 값으로 자동 계산되는 칸이고, **체크박스**는 눌러서 켜고 끄는
네모 칸입니다.

⚠️ **기본 설정에서는 복습 예정일 "당일"에만 보입니다.** 그날 노션을 열지 않고
넘어가면, 다음 날에는 그 기록이 목록에서 사라집니다. 이것은 고장이 아니라 기본
동작입니다.

**밀린 복습까지 보고 싶다면** 이 화면의 필터를 아래처럼 바꾸세요.

1. 오늘의 복습 화면 **우측 상단의 `···`**(뷰 설정) > `필터`를 누릅니다.
2. `복습예정일` 조건을 **`On or before`(당일 또는 그 이전) → `Today`(오늘)**로
   바꿉니다. (영문 노션 기준 표기입니다.)
3. `+ 필터 추가`를 눌러 **`복습완료`가 체크되지 않은 것**만 보이게 조건을 하나 더
   넣습니다.

이렇게 바꾸면 복습 예정일이 지났는데 아직 복습하지 않은 기록이 계속 남아 있습니다.
복습을 마친 기록은 `복습완료`를 체크하면 목록에서 빠집니다.

### 5-7. 최근 추출본

텔레그램에서 `/export`를 보내 `[📄 노션에 저장]`을 누르면 만들어지는 정리본이
여기에 쌓입니다. §3-4를 마쳐야 이 화면에 나타납니다. `/export` 사용법은 README
§5-5에 있습니다.

---

## 🚨 6. 자주 묻는 질문

| 질문 | 답 |
| --- | --- |
| 무료 템플릿에 쌓아 둔 기록을 옮길 수 있나요? | 자동으로 옮겨지지 않습니다. 예전 표는 지워지지 않고 그대로 남아 있습니다 (§3-2) |
| `/setup` 목록에 표가 여러 개 보여요 | `오답노트 저장소`를 고르세요. 다른 표를 고르면 봇이 규격이 맞지 않는다고 알려 주고 막으므로, 기록이 망가지지는 않습니다 |
| 노션에 추출본 표가 하나 더 생겼어요 | `NOTION_EXPORT_DB_ID`가 비어 있거나 틀린 주소였습니다. 아래 "추출본 표가 하나 더 생겼을 때"를 보세요 |
| 오늘의 복습이 비어 있어요 | 기록한 지 정확히 7일째인 기록이 없으면 비어 있는 것이 정상입니다. 밀린 복습까지 보려면 §5-6의 필터 변경을 하세요 |
| 그래프 자리에 잠금 표시가 떠요 | 노션 무료 요금제의 그래프 개수 제한입니다 (§8-2) |
| 디데이에 `시험일을 입력하세요`가 떠요 | 설정 카드의 `시험일`이 비어 있습니다 (§4-1) |
| 취약 단원 순서가 건수 순이 아니에요 | 노션에 건수 기준 자동 정렬 기능이 없어서입니다. 각 단원의 숫자는 정확합니다 (§5-5) |
| 과목을 추가했는데 과목별 탭이 없어요 | 과목별 뷰 탭은 자동으로 생기지 않습니다. 기존 탭을 복제해 필터만 바꾸세요 (§7-3) |
| 봇은 저장했다는데 대시보드에 안 보여요 | 노션 화면이 새 기록을 바로 반영하지 못한 것입니다. `Cmd + R`(윈도우는 `Ctrl + R`)로 새로고침하세요 |
| 속성 이름을 바꿨더니 저장이 안 돼요 | 원래 이름으로 글자 하나까지 똑같이 되돌리세요 (§8-1). 어디가 어긋났는지는 `python check_notion.py`로 확인할 수 있습니다 |

### 추출본 표가 하나 더 생겼을 때

`NOTION_EXPORT_DB_ID`를 채우기 전에 `[📄 노션에 저장]`을 누르면 생깁니다. 이때 봇은
새로 만든 표의 주소를 `.env`에 이미 적어 두었으므로, 그 값을 바꿔 주어야 합니다.

1. §3-4의 방법으로 `🗄 데이터 보관소` 안 `📤 CPA 추출본`의 주소를 복사합니다.
2. `.env`의 `NOTION_EXPORT_DB_ID=` 뒤에 적혀 있는 값을 지우고, 복사한 주소로
   바꿔 적은 뒤 저장합니다.
3. 봇을 껐다 켭니다.
4. 메인 페이지 아래쪽에 새로 생긴 `CPA 추출본` 표를 확인합니다. 그 안의 정리본이
   필요 없다면 표를 지웁니다. (`/export`를 다시 하면 같은 정리본을 새로 만들 수
   있습니다.)

---

## 🧑‍💻 7. 커스터마이즈

### 7-1. 색·문구 바꾸기

아래는 **자유롭게 바꿔도 봇에 영향이 없습니다.**

- 페이지 제목, 안내 문구, 아이콘, 커버 이미지
- 대시보드 안 화면들의 배치
- 선택지(옵션)의 이름·색깔·순서 — 자세한 기준은 README §2-3
- 각 화면에서 속성을 보여 주는 순서, 숨기기

오늘의 한마디 문구를 바꾸는 방법: 오늘의 한마디를 클릭해 코드 수정으로 들어가 따옴표 내에서 직접 문구 수정이 가능합니다.

바꾸면 안 되는 것은 §8-1에 모아 두었습니다.

### 7-2. 복습 주기 바꾸기

기본은 기록일로부터 7일 뒤입니다. `복습예정일` 수식의 숫자 `7`을 바꾸면 됩니다.

1. `오답노트 저장소` 표를 엽니다.
2. `복습예정일` 칸의 제목을 누르고 수식 편집을 엽니다.
3. `dateAdd(prop("기록일"), 7, "days")`에서 `7`만 원하는 숫자로 바꿉니다.
   (예: 3일 뒤라면 `dateAdd(prop("기록일"), 3, "days")`)
4. `완료`를 누릅니다.

⚠️ 수식 안의 `기록일`과 속성 이름 `복습예정일`은 그대로 두세요. 숫자만 바꿉니다.
이 칸은 봇이 쓰지 않으므로 숫자를 바꿔도 봇에는 영향이 없습니다.

### 7-3. 과목 추가하기

1. 봇 쪽의 `chapters.yaml`에 과목·단원을 추가하고 봇을 껐다 켭니다. (README §8-1)
2. 새 과목·단원으로 첫 기록을 저장하면, 노션이 선택지를 자동으로 만들고 취약 단원
   분석(그래프·보드)에도 자동으로 나타납니다.

**단 하나의 예외:** `오답노트 저장소` 표를 직접 열었을 때 위쪽에 보이는
`단원별-○○` 같은 **과목별 뷰 탭은 자동으로 생기지 않습니다.** 필요하면 기존 탭을
복제해 필터만 바꾸세요.

1. `오답노트 저장소` 표를 엽니다.
2. 기존 `단원별-○○` 탭을 누르고, 나타나는 메뉴에서 `복제`를 누릅니다.
3. 새로 생긴 탭의 이름을 새 과목에 맞게 바꿉니다. (예: `단원별-원가`)
4. 그 탭의 `···`(뷰 설정) > `필터`에서 `과목` 조건의 값을 새 과목으로 바꿉니다.

회독수 트래커(§5-4)도 새 과목의 행을 직접 추가해야 합니다.

---

## 8. 주의사항

### 8-1. ⚠️ 속성 이름과 타입은 절대 바꾸지 마세요

**속성**은 표의 칸(열)입니다. 예를 들어 `과목`, `단원`, `오답유형`이 각각 속성입니다.

`오답노트 저장소`와 `📤 CPA 추출본`의 **속성 이름과 타입(칸의 종류)은 봇과의
약속입니다.** 봇은 기록을 넣을 때 속성 이름을 **글자 그대로** 찾습니다. 그래서
`오답유형`을 `오답 유형`으로 한 칸만 띄워도, 선택형을 텍스트형으로 바꿔도 봇이 칸을
찾지 못해 **저장이 실패합니다.** 어디가 어긋났는지 눈으로 찾기가 매우 어려운 종류의
고장이므로, 처음부터 손대지 않는 것이 안전합니다.

지켜야 할 속성의 정확한 목록은 [노션 템플릿 명세서](./docs/notion_template_spec.md)에
있습니다. `오답노트 저장소`는 §1(속성 10개), `CPA 추출본`은 §17-2(속성 5개)를
보세요. 바꿔도 되는 것과 안 되는 것의 기준은 같은 문서의 §5에 있습니다.

반대로 아래는 **자유롭게 바꿔도 됩니다.**

- 속성을 **보여 주는 순서**
- 선택지의 **색깔**
- 화면에서 속성을 **숨기기**

이 템플릿에만 있는 `복습예정일`과 `복습완료`(§5-6)는 봇이 쓰지 않으므로 이름을
바꿔도 저장은 실패하지 않습니다. 다만 `오늘의 복습` 화면의 필터가 이 이름을 쓰고
있으므로, 바꾸면 그 화면이 제대로 동작하지 않습니다.

### 8-2. 무료 요금제의 그래프 제한

노션 무료 요금제에서는 **워크스페이스 전체에서 그래프(차트 뷰)를 1개까지만** 볼 수
있습니다. 이 템플릿은 그 제한에 맞춰 그래프를 **일부러 1개만** 넣었습니다.

그래서 내 노션의 다른 페이지에서 이미 그래프를 쓰고 있다면, 이 템플릿의 그래프가
잠겨 보일 수 있습니다. **휴지통에 들어 있는 그래프도 개수에 포함됩니다.**

- **그래프를 살리려면:** 다른 페이지에서 쓰던 그래프 뷰를 지운 뒤, 노션 왼쪽
  사이드바의 `휴지통`에서도 그 페이지나 뷰를 영구 삭제합니다.
- **그래프가 없어도 괜찮습니다.** 취약 단원 분석의 그래프 바로 아래 보드에 같은
  정보(단원별 건수)가 나옵니다. 보드는 요금제 제한이 없습니다.

그래프를 쓰지 않던 무료 계정에서는 정상으로 표시되는 것을 확인했습니다.

### 8-3. 알려진 한계

미리 알고 쓰시면 당황하지 않습니다.

| 한계 | 내용 | 대처 |
| --- | --- | --- |
| 그래프 1개 | 노션 무료 요금제 제한입니다 | 보드로 같은 정보를 봅니다 (§8-2) |
| 단원 순서 | 노션에 건수 기준 자동 정렬 기능이 없어, 순서는 미리 정해 둔 배치입니다 | 건수 숫자는 항상 정확하니 숫자를 봅니다 (§5-5) |
| 복습 누락 | 기본 필터는 7일째 당일만 보여 줍니다. 그날 열지 않으면 목록에서 사라집니다 | 필터를 바꿔 밀린 복습까지 봅니다 (§5-6) |
| 월간 목표 필터 | `월` 속성이 선택형이라 이번 달을 자동으로 알지 못합니다 | 매달 필터를 직접 바꿉니다 (§5-3) |
| 과목별 뷰 탭 | 과목을 추가해도 `단원별-○○` 탭은 자동으로 생기지 않습니다 | 기존 탭을 복제해 필터만 바꿉니다 (§7-3) |

---

## 부록

### 라이선스

이 템플릿은 **크리에이티브 커먼즈 저작자표시-비영리-변경금지 4.0(CC BY-NC-ND 4.0)**
조건으로 제공됩니다. 상업적으로 이용하거나, 고친 것을 다른 사람에게 배포할 수
없습니다. 정확한 조건은 [라이선스 전문](https://creativecommons.org/licenses/by-nc-nd/4.0/deed.ko)을
확인하세요.

### 문의

막히는 지점이나 오류는
[GitHub Issues](https://github.com/git-Jynx/ErrorLog-Bot/issues)(이 프로젝트의 질문·오류
게시판)에 올려 주세요. 봇 오류라면 터미널에 나온 **오류 원문을 그대로 복사해서** 함께
적어 주시면 원인을 빨리 찾을 수 있습니다.

⚠️ 글을 올릴 때 `.env`에 적힌 토큰 값이나 노션 표 주소는 **지우고** 올려 주세요.
